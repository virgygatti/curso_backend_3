# MongoDB. Local por defecto; para Atlas usar tu connection string.
MONGODB_URI=mongodb://localhost:27017/backend1

# Puerto del servidor (opcional, por defecto 8080)
# PORT=8080

# JWT (sesiones y estrategia "current")
JWT_SECRET=clave-secreta-muy-segura
JWT_EXPIRES=24h
JWT_COOKIE_NAME=token